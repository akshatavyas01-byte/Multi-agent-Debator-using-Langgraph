AI MULTIAGENT DEBATOR

This projects consits of multiple agents like pro-agent, con-agent, factchecker agent and judge-agent. The pro and con agent goes against each other for 5 rounds, the facts are checked by factchecker agent based on the  integrity of the facts the judge agent decides the winner. Each agent has it own properties. 